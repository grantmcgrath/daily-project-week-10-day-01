import React, {Component} from 'react';

export default class Header extends Component {
  render() {
    return (
      <nav className = "">
        <a href = "." className = "link">I am the Navigation Bar</a>
      </nav>
    );
  }
}
